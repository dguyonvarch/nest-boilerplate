import appConfig, { AppConfig } from './app.configuration';
import authConfig, { AuthConfig } from './auth.configuration';
import dbConfig, { DbConfig } from './db.configuration';
import loggerConfig, { LoggerConfig } from './logger.configuration';

export {
  AppConfig,
  appConfig,
  AuthConfig,
  authConfig,
  DbConfig,
  dbConfig,
  LoggerConfig,
  loggerConfig,
};
